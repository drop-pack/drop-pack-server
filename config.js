const port = 8000
const keyLocation = 'cert/key.pem'
const certLocation = 'cert/cert.pem'
const hostname = "localhost"
const accountPassword = "test"

module.exports = {
    port,
    keyLocation,
    certLocation,
    hostname,
    accountPassword
}